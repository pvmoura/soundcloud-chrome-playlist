function playRadio (artist) {
  // based on a given artist, start a radio station 
}

function followArtist (artist) {
  // add artist to users' followed list
}

function populateRadioChoices () {
  // Create a list of artists the user follows
}

function changeSettings (settings) {
  // update settings
}